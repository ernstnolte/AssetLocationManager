# Location Storage
## Setup
The project is set up as a ready-to-launch package. The following is required to start the webserver:
1. Run (Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned) ; (& .\venv\Scripts\Activate.ps1)
2. Run "python manage.py runserver 0.0.0.0:8000"